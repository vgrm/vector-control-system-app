import UserList from '../components/User/UserList'

const UserListPage = (props) => {

    return (
        <div>
            <UserList />
        USER PAGE
        </div>
    );

}

export default UserListPage;